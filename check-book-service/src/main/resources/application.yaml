server:
  port: 8082
spring:
  application:
    name: check-book-service
  cloud:
    config:
      enabled: false
    stream:
      kafka:
        binder:
          auto-add-partitions: true
  profiles:
    active: native


