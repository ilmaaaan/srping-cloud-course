server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka

spring:
  application:
    name: client-service
  #  config:
  #    import: 'optional:configserver:'
  cloud:
    config:
      enabled: false
  profiles:
    active: native



